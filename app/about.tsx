import RootView from "@/components/RootView";
import ThedText from "@/components/ThedText";

export default function About() {
  return (
    <RootView>
      <ThedText>
        This is the about screen. Edit app/about.tsx to change this screen.
      </ThedText>
    </RootView>
  );
}
